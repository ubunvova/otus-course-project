monolog:
  handlers:
    main:
      type: stream
      path: "%kernel.logs_dir%/%kernel.environment%.log"
      level: debug

    consumer_stdout:
      type: stream
      path: php://stdout
      level: debug
      channels: ["!event", "!doctrine", "!console", "!deprecation"]
